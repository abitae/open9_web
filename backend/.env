# MySQL DSN (user:password@tcp(host:port)/dbname?parseTime=true)
DATABASE_URL=root:lobomalo123@tcp(127.0.0.1:3306)/open9?parseTime=true

# Secret to sign JWT tokens (use a strong value in production)
JWT_SECRET=4be2e3ad-1436-4818-a4b8-acc26e0bdb57

# Optional: default admin password when seeding the first user (username: admin)
ADMIN_DEFAULT_PASSWORD=lobomalo123

# Optional: server port (default 8080)
PORT=8083
